"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ClickController');

describe('controllers:ClickController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
