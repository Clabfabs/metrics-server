"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Click');
const Fixture = require('../../fixtures/ClickFixture');

describe('models:Click', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
